# Attacks

Attack simulation scripts.

## Brute Forcer

```bash
# Target single user with wordlist
python3 brute_forcer/brute_forcer.py -u weak_user_01
```

## Password Sprayer

```bash
# Try common passwords against all users
python3 password_sprayer/password_sprayer.py
```

See each subdirectory for options.
